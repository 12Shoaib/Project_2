import {atom} from 'recoil'

export const atomState = atom({
    key:'Shoaib',
    default: [{
        
    }],
}) 

export const isLogin =  atom({
    key:'islogin',
    default : false
})


export const twenty =  atom({
    key:'twenty',
    default : false

})


export const thirty =  atom({
    key:'thirty',
    default : false

})

export const flag =  atom({
    key:'thirty',
    default : false

})